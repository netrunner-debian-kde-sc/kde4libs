--- kde4libs-4.0.84+svn828171.orig/kdecore/kernel/kstandarddirs.cpp
+++ kde4libs-4.0.84+svn828171/kdecore/kernel/kstandarddirs.cpp
@@ -1011,6 +1011,10 @@
                         if ((local || testdir.exists()) && !candidates.contains(path))
                             candidates.append(path);
                     }
+                    // UGLY HACK - forward porting Chris CHeney's HACK - Rex Dieter
+                    if ( local && (!strcmp("config", type)))  // local is used as a "homedir marker"
+                        candidates.append("/etc/kde4/");
+                    //
                     local = false;
                 }
 	        else
